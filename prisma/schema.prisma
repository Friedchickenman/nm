// prisma/schema.prisma

// 1. Prisma 클라이언트 설정 (건드리지 마세요!)
generator client {
  provider = "prisma-client-js"
}

// 2. DB 연결 설정 (나중에 .env 파일에 주소만 넣으면 됨)
datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

// ==========================================
// 📌 여기서부터 테이블(Entity) 정의 시작!
// ==========================================

// 3. 사용자 (User) - NextAuth(로그인) 필수 모델
model User {
  id            String    @id @default(cuid()) // PK (UUID 자동생성)
  name          String?   // 닉네임
  email         String?   @unique // 이메일 (중복 불가)
  emailVerified DateTime?
  image         String?   // 프로필 사진 URL

  accounts      Account[] // 소셜 로그인 정보 (1:N)
  sessions      Session[] // 로그인 세션 (1:N)
  reviews       Review[]  // 유저가 쓴 리뷰들 (1:N)

  createdAt     DateTime  @default(now()) // 가입일
  updatedAt     DateTime  @updatedAt      // 수정일
}

// 4. 소셜 로그인 계정 정보 (카카오, 구글 등)
model Account {
  id                 String  @id @default(cuid())
  userId             String
  type               String
  provider           String  // "kakao", "google" 등
  providerAccountId  String  // 해당 플랫폼에서의 ID
  refresh_token      String? @db.Text
  access_token       String? @db.Text
  expires_at         Int?
  token_type         String?
  scope              String?
  id_token           String? @db.Text
  session_state      String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
}

// 5. 로그인 세션 관리
model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
}

// 6. 🎬 영화 리뷰 (핵심 기능!)
model Review {
  id         String   @id @default(cuid())

  // 핵심 데이터
  movieId    Int      // TMDB ID (숫자 그대로 쓰는 게 API 연동 시 편함)
  waterLevel Int      // 비커에 담긴 물의 양 (0~500ml로 관리)
  content    String?  @db.Text // 한 줄 평 (선택사항)

  // 관계 설정
  userId     String
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  createdAt  DateTime @default(now())

  @@index([movieId])
  @@index([userId])
}

// 7. 이메일 인증 토큰 (로그인용)
model VerificationToken {
  identifier String
  token      String   @unique
  expires    DateTime

  @@unique([identifier, token])
}